(()=>{const tabs=document.querySelectorAll(".floor-tab");const panels=document.querySelectorAll(".floor-panel");function showFloor(id){tabs.forEach(t=>t.classList.toggle("is-active",t.dataset.floor===id));panels.forEach(p=>p.classList.toggle("is-active",p.dataset.floorPanel===id))}tabs.forEach(t=>t.addEventListener("click",()=>showFloor(t.dataset.floor)));document.querySelectorAll(".floor-panel").forEach(panel=>{function setActive(id,state){panel.querySelectorAll(`[data-apt="${id}"],[data-apt-list="${id}"]`).forEach(el=>el.classList.toggle("is-active",state))}panel.querySelectorAll(".apt-link,[data-apt-list]").forEach(el=>{const id=el.dataset.apt||el.dataset.aptList;el.addEventListener("mouseenter",()=>setActive(id,true));el.addEventListener("mouseleave",()=>setActive(id,false));el.addEventListener("focus",()=>setActive(id,true));el.addEventListener("blur",()=>setActive(id,false))})})})();